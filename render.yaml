services:
  - type: web
    name: almas-api
    runtime: python
    buildCommand: pip install -r requirements-api.txt
    startCommand: gunicorn api:app
    envVars:
      - key: OPENCODE_API_KEY
        sync: false
